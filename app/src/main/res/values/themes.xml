<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidNativeApp1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AndroidNativeApp1" parent="Base.Theme.AndroidNativeApp1" />

    <style name="AppBarTitleStyle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">18sp</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="DrawerItemCollapse">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">#147B72</item>
    </style>

    <style name="Theme.AndroidNativeApp1.Fullscreen" parent="Theme.AndroidNativeApp1">
        <item name="android:actionBarStyle">
            @style/Widget.Theme.AndroidNativeApp1.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.AndroidNativeApp1.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>

</resources>